/*
A declaration :  仅指出类型
A definition ：指出类型的同时，为每个对象分配物理空间。
引用性声明与定义性声明的区别：
 ●引用性声明只将名字与类型联系起来，而定义则给出了名字所引用的实体。
 ●一个对象可以多次引用性声明，但只能一次定义性声明。
*/
#include "header.h"
int a;	//global variable definition

void main()
{
	float a = j+size; //local variable definition
	fun();

}

float j=(float)2.2;	  //global variable definition

int gun()
{
	int sum;
	sum = 0;
	int i;
	for(i=1;i<=100;i++)
		sum += i;
	return sum;
}

