#include <stdio.h>
int main(void)
{
    int i, n;
    int looptimes, ri;
    double distance, height; 

    scanf("%d", &looptimes);
    for(ri = 1; ri <= looptimes; ri++){
        scanf("%lf%d", &height, &n);
        if(n==1){
        	
        }
      	printf("distance = %.1f, height = %.1f\n", distance, height);
   }
}
